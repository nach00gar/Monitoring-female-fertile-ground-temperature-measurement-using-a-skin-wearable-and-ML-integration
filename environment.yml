name: ovulation
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.10
  - pip
  - numpy<2             # Pinned: numpy 2.x breaks legacy pickle compatibility
  - scipy>=1.10
  - pandas>=2.0
  - scikit-learn>=1.3
  - matplotlib>=3.7
  - statsmodels>=0.14
  - pyyaml>=6.0
  - pip:
      - jupyter
      - ipykernel
